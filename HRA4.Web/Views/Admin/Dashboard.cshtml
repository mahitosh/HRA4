@model IEnumerable<HRA4.Entities.Institution>

@{
    ViewBag.Title = "Dashboard";
}

@section breadcrumbs{
    <div class="clearfix header-bdr"><hr /></div>
    <div>
        <div class="container">
            <div class="col-sm-4 top-breadcrumb">Home > Home Dashbaord</div>
            <div class="col-lg-5 col-md-6 col-sm-7 help pull-right">
                <a href="#">Help <img src="~/images/help-ico.svg" width="25" height="25" alt="logout" /></a>
            </div>
        </div>
    </div>   }
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InstitutionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DbName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DbUserName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DbPassword)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Configuration)
        </th>
        <th></th>
    </tr>
    <tr>
        partial view for Add
    </tr>
@foreach (var item in Model) {
    <tr>
        partial view for edit
    </tr>
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InstitutionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DbName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DbUserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DbPassword)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Configuration)
        </td>
        <td>
            @Html.ActionLink("Select", "InstitutionDashboard", "Institution", new { id = item.Id }, new { id = item.Id })           
        </td>
    </tr>
}

    <div>
        @Html.ActionLink("Add Institution", "AddInstitution")
    </div>

</table>
