using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using WeifenLuo.WinFormsUI.Docking;

namespace RiskApps3.View.Common
{
    class DockingUtils
    {
        public static IDockContent GetContentFromPersistString(string persistString)
        {
            //if (persistString == typeof(HighRiskFollowupView).ToString())
            //{
            //    hrfv = new HighRiskFollowupView(queueData, initialFilterSelectionIndex);
            //    hrfv.Register(sessionManager);
            //    return hrfv;
            //}
            //if (persistString == typeof (PatientCommunicationView).ToString())
            //{
            //    pcv = new PatientCommunicationView();
            //    pcv.Register(sessionManager);
            //    return pcv;
            //}

            return null;
        }
    }
}
